name: Frontend Linting and Formatting
on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

jobs:
  lint:
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 14
      - run: npm install
      - run: npm test
