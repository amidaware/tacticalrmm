on:
  push:
    branches:
      - ["develop"]
  pull_request:
    branches:
      - ["develop"]

jobs:
  steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
      with:
        node-version: 16
    - run: npm install
    - run: npm lint
    - run: npm format
